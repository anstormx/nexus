/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets-cdn.trustwallet.com', 'raw.githubusercontent.com', 'assets.coingecko.com', 'ethereum-optimism.github.io'],
    },
};

export default nextConfig;
